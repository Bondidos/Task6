<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graf"
    app:startDestination="@id/listFragment">

    <fragment
        android:id="@+id/listFragment"
        android:name="com.bondidos.task6.fragments.ListFragment"
        android:label="ListFragment" />
    <fragment
        android:id="@+id/songFragment"
        android:name="com.bondidos.task6.fragments.SongFragment"
        android:label="SongFragment" />

    <action android:id="@+id/globalActionToSongFragment"
        app:destination="@id/songFragment"/>
    <action
        android:id="@+id/navToListFragment"
        app:destination="@id/listFragment" />
</navigation>